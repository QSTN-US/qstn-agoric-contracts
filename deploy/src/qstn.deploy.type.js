// import { name as contractName } from './qstn.contract.permit.js';

/**
 * @import {start as StartFn} from '../../contract/src/qstn.contract.js';
 * @import {CorePowersG} from '../tools/orch.start.types.js';
 * @import {name as contractName, permit} from './qstn.contract.permit.js';
 * @import {Issuer} from '@agoric/ertp';
 */

/**
 * @typedef { CorePowersG<contractName, StartFn, permit> &{
 * issuer: {
 *  consume: {BLD: Promise<Issuer>, IST: Promise<Issuer>}
 * }}} QstnBootPowers
 */

export {};
